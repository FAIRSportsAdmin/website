import type React from "react"
import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "AI Clause Builder - FAIR Sports",
  description: "Build custom dispute resolution clauses with AI assistance - Coming Soon",
}

export default function AIClauseBuilderLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
